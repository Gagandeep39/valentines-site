/**
 * Main initialization
 * Loads all modules and initializes the app
 */
document.addEventListener('DOMContentLoaded', function() {
    initStarField();
    populateContent();
    initSlideManager();
});
